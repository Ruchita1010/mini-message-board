<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./style.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('partials/header', {link}) %>
    <div class="messages">
      <% messages.forEach(message => { %>
      <div class="message">
        <p class="message-details">
          @<%=message.username%>
          <span class="date">&lt;<%=message.created_on%>&gt;</span>
        </p>
        <p><span class="hyphen">-</span> <%=message.message%></p>
      </div>
      <%})%>
    </div>
  </body>
</html>
